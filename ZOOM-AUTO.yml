pipeline {
  agent any
  
  stages {
    stage('Installing ZOOM') {
      steps {
        sh 'ansible-playbook -i inventory/web.env  --private-key=$ANSIBLE_PRIVATE_KEY  playbooks/Testing.yml StrictHostKeyChecking=no'
      }
    }
  }
}
