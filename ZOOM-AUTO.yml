pipeline {
  agent any
  
  stages {
    stage('Installing Cockpit from GIT') {
      steps {
        sh 'ansible-playbook -i inventory/web.env  --private-key=$ANSIBLE_PRIVATE_KEY playbooks/zoom.yml'
      }
    }
  }
}
