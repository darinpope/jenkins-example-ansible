 - name: Installing Nomachine
   hosts: webservers
   gather_facts: false
   become: yes
   tasks:
   - name: Nomachine >>>><<<<
     yum:
       name: 'yum install vlc'
       state: present
     become: yes
