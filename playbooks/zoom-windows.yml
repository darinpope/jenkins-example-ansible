 - name: ZOOM Testing
   hosts: win
   gather_facts: false
   become: yes
   tasks:
   - name: Downloading ZOOM
     win_package:
       path: 'https://zoom.us/client/5.9.3.3169/ZoomInstaller.exe?archType=x64'
       state: present
     become: yes 
       ansible_become_method=runas
