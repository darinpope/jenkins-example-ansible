 - name: ZOOM Testing
   hosts: win
   gather_facts: false
   become: yes
   tasks:
   - name: Downloading ZOOM
     win_get_url:
         url: 
        dest: C:\temp\ZoomInstaller.exe
     become: yes 
       ansible_become_method=Administrator
