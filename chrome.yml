pipeline {
  agent any
  
  stages {
    stage('Installing Google') {
      steps {
        sh 'ansible-playbook -i inventory/web.env  --private-key=$ANSIBLE_PRIVATE_KEY playbooks/chrome.yml'
      }
    }
  }
}
