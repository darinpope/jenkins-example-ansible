pipeline {
  agent any
  environment {
   ANSIBLE_PRIVATE_KEY=credentials('mariadb-private-key') 
  }
  stages {
    stage('Hello') {
      steps {
        sh 'ansible-playbook -i inventory/web.env --private-key=$ANSIBLE_PRIVATE_KEY playbooks/play.yml'
      }
    }
  }
}
